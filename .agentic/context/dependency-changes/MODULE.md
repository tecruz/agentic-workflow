# Dependency Changes Module

## Trigger Rules
- Changes to `package.json`, `Cargo.toml`, `requirements.txt`, `pom.xml`, or lockfiles.

## Guidelines
1. Verify license compatibility and supply-chain integrity.
2. Require user approval before adding or upgrading dependencies.
3. Run project verification suite post-update.
